version: 2.1
orbs:
  azure-cli: circleci/azure-cli@1.2.0
jobs:
  test-azure-cli-install:
    docker:
      - image: circleci/node
    steps:
      - azure-cli/install
                
  endpointTest:
    jobs:
      - test-azure-cli-install
  
